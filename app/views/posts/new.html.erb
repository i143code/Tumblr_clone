<div id="page_wrapper">
	<%=form_for @post do |t| %>
	<p><%= t.label :title %>
	<%= t.text_field :title %></p>
	<p><%= t.label :body %>
	 <%= t.text_area :body %></p>
	<p><%= t.submit%></p>
<% end %>
</div>

